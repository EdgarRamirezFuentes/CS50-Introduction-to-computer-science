#include <stdio.h>
// Only works on CS5 IDE
//#include <cs50.h>

int main(void)
{
    // Only works on CS50 IDE
    string name = "";
    // Get name from user
    name = get_string("What's your name? ");
    printf("hello, %s\n", name);
}